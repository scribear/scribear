# Copy to ./local-dev.env (gitignored) for scripts/start-local-stack.sh + docker-compose.local.yml

COMPOSE_POSTGRES_DB=scribear-db-dev
COMPOSE_POSTGRES_USER=scribear
COMPOSE_POSTGRES_PASSWORD=scribeardev
COMPOSE_POSTGRES_PORT=5432

COMPOSE_REDIS_PASSWORD=scribeardev
COMPOSE_REDIS_PORT=6379

# Used only when the start script creates missing app .env files (JWT must match SM + NS)
JWT_SECRET=local-dev-jwt-secret-change-me-32chars-minimum!!

API_KEY=local-dev-api-key
NODE_SERVER_KEY=local-dev-node-key
TRANSCRIPTION_API_KEY=local-dev-transcription-key
