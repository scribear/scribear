// Milliseconds of no interaction after which the user is considered inactive.
export const USER_ACTIVITY_TIMEOUT = 3000;

// Default debounce delay in milliseconds used by `useDebouncedCallback` and `useDebouncedValue`.
export const DEFAULT_DEBOUNCE_DELAY = 100;
