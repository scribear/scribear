#### Log level to use
LOG_LEVEL=info

#### Host and port server should listen on
HOST=0.0.0.0
PORT=8000

#### JWT Configuration
# Secret key for signing JWT tokens (minimum 32 characters)
# IMPORTANT: Generate a secure random string for production!
# Example: openssl rand -base64 32
JWT_SECRET=your-secret-key-min-32-chars-change-me-in-production

# JWT issuer (optional, defaults to 'scribear-session-manager')
# JWT_ISSUER=scribear-session-manager

# JWT token expiration time (optional, defaults to '24h')
# Examples: '1h', '7d', '30m'
# JWT_EXPIRES_IN=24h
