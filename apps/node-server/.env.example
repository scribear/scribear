#### Log level to use
LOG_LEVEL=info

#### Host and port server should listen on
HOST=0.0.0.0
PORT=8001

#### JWT Configuration (must match session-manager's JWT_SECRET and JWT_ISSUER)
JWT_SECRET=your-secret-key-min-32-chars-change-me-in-production
# JWT_ISSUER=scribear-session-manager

#### Transcription Service
TRANSCRIPTION_SERVICE_URL=localhost:8080
TRANSCRIPTION_API_KEY=your-transcription-api-key

#### Session Manager
SESSION_MANAGER_URL=http://localhost:8000
