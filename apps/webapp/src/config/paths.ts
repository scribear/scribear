/**
 * Defines url paths for different modes of the application
 * Use these constants if needing to link to specific pages in the app
 */

export const PATHS = {
  landing: '/',
  standalone: '/standalone',
  kiosk: '/kiosk',
  client: '/client',
} as const;
